const Database=require("better-sqlite3"),fs=require("fs"),path=require("path"),dbDir=path.join(__dirname,"db");fs.existsSync(dbDir)||fs.mkdirSync(dbDir,{recursive:!0});const db=new Database(path.join(dbDir,"database.sqlite"));db.pragma("foreign_keys = ON"),module.exports=db;